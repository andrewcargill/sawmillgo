import React from "react";

const LogWithoutTree = ({ log }) => {
    const treeId = null;
    

    return (
        <div>
        Log Without Tree Component
        </div>
    );
    };

export default LogWithoutTree;